name : Build testing on push

on:
  workflow_call: 


jobs:
  working-job:
    runs-on: ubuntu-latest

    steps:
      - name : Echo Message
        run : echo "Build and push is working successfully ✅"
