name : Application Deployment on Aws 

on:
  workflow_dispatch

jobs:
  working-job:
    runs-on: ubuntu-latest

    steps:
      - name : Echo Message
        run : echo "Deployment application is working successfully ✅"